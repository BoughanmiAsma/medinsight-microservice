name: DevSecOps CI/CD Pipeline

on:
  push:
    branches: 
      - main
      - feature/DevSecOps
      - feature/deploy
      - feature/deploy2
      - feature/rapport-upload
      - feature/k8s
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  DOCKER_IMAGE_PREFIX: boughanmiasma/pds
  NAMESPACE: medinsight

jobs:
  # ==================== PHASE 1: PRE-COMMIT SECURITY ====================
  secret-scan:
    name: 🔐 Secret Scanning
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: TruffleHog Secret Scan
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --debug --only-verified

  # ==================== PHASE 2: DEPENDENCY SECURITY (SCA) ====================
  dependency-scan:
    name: 📦 Dependency Vulnerability Scan (SCA)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - MedInsight-Microservices/dossier-service
          - MedInsight-Microservices/lab-service
          - MedInsight-Microservices/ordonnance-service
          - MedInsight-Microservices/Staff-service
          - MedInsight-Microservices/appointment-service
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Trivy Dependency Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '${{ matrix.service }}'
          format: 'table'
          vuln-type: 'library'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'

  # ==================== PHASE 3: IAC SECURITY ====================
  iac-security-scan:
    name: 🏗️ Infrastructure Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Trivy K8s Manifests Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'config'
          scan-ref: 'k8s/'
          format: 'table'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'

      - name: Trivy Terraform Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'config'
          scan-ref: 'infra/terraform/'
          format: 'table'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'

  # ==================== PHASE 4: BUILD & SAST ====================
  build-and-analyze:
    name: 🔨 Build & SAST Analysis
    needs: [secret-scan, dependency-scan, iac-security-scan]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service_path:
          - MedInsight-Microservices/dossier-service
          - MedInsight-Microservices/lab-service
          - MedInsight-Microservices/ordonnance-service
          - MedInsight-Microservices/Staff-service
          - MedInsight-Microservices/appointment-service

    services:
      sonarqube:
        image: sonarqube:community
        ports:
          - 9000:9000
        options: >-
          --health-cmd "curl -f http://localhost:9000/api/system/status || exit 1"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 20

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven
        working-directory: ./${{ matrix.service_path }}
        run: |
          echo "Building project at ${{ matrix.service_path }}"
          mvn clean package -DskipTests -B

      - name: Wait for SonarQube & Health Check
        run: |
          echo "Waiting for SonarQube at http://localhost:9000..."
          MAX_RETRIES=40
          COUNT=0
          until curl -s -u admin:admin http://localhost:9000/api/system/status | grep -q "UP"; do
            COUNT=$((COUNT+1))
            if [ $COUNT -ge $MAX_RETRIES ]; then
              echo "Error: SonarQube failed to start after $MAX_RETRIES attempts."
              # Print logs of service container if possible, but here we can't easily.
              # Let's try to check why it failed.
              curl -v http://localhost:9000/api/system/status || true
              exit 1
            fi
            echo "Waiting for SonarQube... ($COUNT/$MAX_RETRIES)"
            sleep 10
          done
          echo "SonarQube is UP and running!"

      - name: Configure SonarQube & Generate Token
        id: sonar-token-gen
        run: |
          NEW_PASS="${{ secrets.SONAR_ADMIN_PASSWORD }}"
          if [ -z "$NEW_PASS" ]; then NEW_PASS="MedInsightSecure2026_"; fi
          
          echo "Attempting to change admin password..."
          CHANGE_PW_STATUS=$(curl -s -o /dev/null -w "%{http_code}" -u admin:admin -X POST "http://localhost:9000/api/users/change_password?login=admin&previousPassword=admin&password=$NEW_PASS")
          echo "Password change HTTP status: $CHANGE_PW_STATUS"
          
          # If 204 or 400 (already changed), proceed
          
          echo "Generating analysis token..."
          TOKEN_NAME="DevSecOps-$(date +%s)"
          TOKEN_JSON=$(curl -s -u admin:$NEW_PASS -X POST "http://localhost:9000/api/user_tokens/generate?name=$TOKEN_NAME&type=GLOBAL_ANALYSIS_TOKEN")
          SONAR_TOKEN=$(echo "$TOKEN_JSON" | jq -r '.token')
          
          if [ "$SONAR_TOKEN" == "null" ] || [ -z "$SONAR_TOKEN" ]; then
            echo "Failed to generate token. API Response: $TOKEN_JSON"
            exit 1
          fi
          
          echo "SONAR_TOKEN=$SONAR_TOKEN" >> $GITHUB_ENV
          echo "Token generated successfully."

      - name: SonarQube SAST Analysis
        working-directory: ./${{ matrix.service_path }}
        run: |
          mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
            -DskipTests \
            -Dsonar.projectKey=$(basename ${{ matrix.service_path }} | tr '[:upper:]' '[:lower:]') \
            -Dsonar.projectName=$(basename ${{ matrix.service_path }}) \
            -Dsonar.host.url=http://localhost:9000 \
            -Dsonar.token="${{ env.SONAR_TOKEN }}"

      - name: Set Service Variables
        run: |
          SERVICE_DIR=$(basename ${{ matrix.service_path }})
          
          if [ "$SERVICE_DIR" == "lab-service" ]; then
            IMAGE_NAME="laboratoire"
            K8S_DIR="laboratoire-service"
          elif [ "$SERVICE_DIR" == "Staff-service" ]; then
            IMAGE_NAME="staff"
            K8S_DIR="staff-service"
          else
            IMAGE_NAME=$(echo $SERVICE_DIR | sed 's/-service//I' | tr '[:upper:]' '[:lower:]')
            K8S_DIR=$SERVICE_DIR
          fi
          
          echo "IMAGE_NAME=$IMAGE_NAME" >> $GITHUB_ENV
          echo "K8S_DIR=$K8S_DIR" >> $GITHUB_ENV
          echo "IMAGE_TAG=${{ env.DOCKER_IMAGE_PREFIX }}:service-$IMAGE_NAME" >> $GITHUB_ENV

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build -t "${{ env.IMAGE_TAG }}" ./${{ matrix.service_path }}

      - name: Trivy Container Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_TAG }}
          format: 'table'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'

      - name: Push Docker Image
        run: |
          docker push "${{ env.IMAGE_TAG }}"

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.MASTER_IP }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            mkdir -p ~/deploy-medinsight
            cd ~/deploy-medinsight
            
            if [ -d ".git" ]; then
              git fetch origin
              git checkout ${{ github.ref_name }}
              git pull origin ${{ github.ref_name }}
            else
              git clone -b ${{ github.ref_name }} https://github.com/BoughanmiAsma/medinsight-microservice.git .
            fi
            
            MANIFEST_PATH="k8s/${{ env.K8S_DIR }}/${{ env.K8S_DIR }}.yml"
            kubectl set image deployment/${{ env.K8S_DIR }} ${{ env.K8S_DIR }}=${{ env.IMAGE_TAG }} -n ${{ env.NAMESPACE }} || true
            kubectl apply -f $MANIFEST_PATH -n ${{ env.NAMESPACE }}
            kubectl rollout status deployment/${{ env.K8S_DIR }} -n ${{ env.NAMESPACE }}

  # ==================== PHASE 5: DAST (Dynamic Testing) ====================
  dast-security-scan:
    name: 🌐 DAST - OWASP ZAP Scan
    needs: build-and-analyze
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.12.0
        with:
          target: 'http://52.205.61.44:8200'
          rules_file_name: '.zap/rules.tsv'
          cmd_options: '-a'
          fail_action: false

  # ==================== PHASE 6: SECURITY REPORT ====================
  security-report:
    name: 📊 Security Summary
    needs: [secret-scan, dependency-scan, iac-security-scan, build-and-analyze]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Generate Security Report
        run: |
          echo "## 🛡️ DevSecOps Security Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Phase | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🔐 Secret Scan | ${{ needs.secret-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 📦 Dependency Scan | ${{ needs.dependency-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🏗️ IaC Security | ${{ needs.iac-security-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔨 Build & SAST | ${{ needs.build-and-analyze.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Pipeline:** DevSecOps CI/CD" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
