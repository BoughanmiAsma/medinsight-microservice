apiVersion: v1
kind: Secret
metadata:
  name: ordonnance-secret
  namespace: medinsight
type: Opaque
data:
  DB_PASSWORD: cGFzc3dvcmQ= # password
  KEYCLOAK_CLIENT_SECRET: Z3VpSkc4aHJDa2ZINXUyRWd2SEhqY0RRUkI2eEpTZHM=
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ordonnance-config
  namespace: medinsight
data:
  SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/medinsight_platform
  SPRING_DATASOURCE_USERNAME: admin
  SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
  KEYCLOAK_AUTH_SERVER_URL: http://keycloak:8180
  KEYCLOAK_REALM: medinsight-realm
  KEYCLOAK_RESOURCE: ordonnance-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ordonnance-service
  namespace: medinsight
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ordonnance-service
  template:
    metadata:
      labels:
        app: ordonnance-service
    spec:
      containers:
      - name: ordonnance-service
        image: boughanmiasma/pds:service-ordonnance
        ports:
        - containerPort: 8082
        envFrom:
        - configMapRef:
            name: ordonnance-config
        env:
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ordonnance-secret
              key: DB_PASSWORD
        - name: KEYCLOAK_CREDENTIALS_SECRET
          valueFrom:
            secretKeyRef:
              name: ordonnance-secret
              key: KEYCLOAK_CLIENT_SECRET
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8082
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: ordonnance-service
  namespace: medinsight
spec:
  selector:
    app: ordonnance-service
  ports:
  - port: 8082
    targetPort: 8082
    nodePort: 30082
  type: NodePort

